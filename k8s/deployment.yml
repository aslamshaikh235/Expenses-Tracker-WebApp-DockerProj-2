apiVersion: apps/v1
kind: Deployment
metadata:
  name: expenses-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: expenses
  template:
    metadata:
      labels:
        app: expenses
    spec:
      containers:
      - name: expenses-container
        image: aslamshaikh235/expenses-app:latest
          ports:
          - containerPort: 8080

